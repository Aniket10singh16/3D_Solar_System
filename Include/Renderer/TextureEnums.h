#pragma once

namespace Render {

    enum class TextureType {
        Diffuse,
        Specular,
        Normal,
        Emissive,
        Height,
        Roughness,
        Metalness,
        AmbientOcclusion
    };

}
